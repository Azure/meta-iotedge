export VERSION = "1.5.21"
IOTEDGE_RELEASE = "1.5.35"
IIS_SRCREV = "833381accec8d53436cac20fc3fb85303e4504eb"

# Source-compatibility patches for the 1.5.21 edgelet workspace. The set is
# version-specific (the upstream Cargo.toml and source layout differ by
# release), so it is wired here rather than in the shared aziot-edged.inc. This
# lets 1.5.x and 1.6.x recipes coexist on the same branch. Generated by
# scripts/update-recipes.sh from the patch files in the recipe's patch dir.
SRC_URI += "file://0001-Remove-panic-abort-from-workspace-profiles.patch \
            file://0002-Work-around-Rust-1.78-dead_code-lint-issues.patch \
"
